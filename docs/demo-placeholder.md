# Demo Placeholder

Asciinema walkthrough coming soon.

Planned recording flow:

1. Run `kag --doctor`
2. Run `kag`
3. Search and select a competition
4. Show download + editor selection
5. Show created project structure (`data/`, notebook, `notes.md`)
6. Show automatic `cd` behavior with shell hook

Planned cast filename: `docs/demo.cast`
